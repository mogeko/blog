---
const { class: className, ...props } = Astro.props;

type Props = astroHTML.JSX.AnchorHTMLAttributes;
---

<a
  class:list={[
    "relative font-bold text-primary underline-offset-4 hover:underline",
    className,
  ]}
  {...props}
>
  <slot />
</a>

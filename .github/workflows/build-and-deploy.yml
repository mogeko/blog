name: Build & Deploy
on: [push, pull_request]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master
      with:
          submodules: true
          fetch-depth: 1
    - name: Build
      uses: jakejarvis/hugo-build-action@master
    - name: Deploy
      uses: Mogeko/git-push@master
      with:
        GITHUB_EMAIL: zhengjunyi@live.com
        GITHUB_USERNAME: Mogeko
        GIT_COMMIT: Update Blog By TravisCI With Build $TRAVIS_BUILD_NUMBER
        GIT_REPO: github.com/Mogeko/mogeko.github.io
        GIT_BRANCH: master
        WORK_DIR: "./public"


